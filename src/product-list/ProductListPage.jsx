// Placeholder page for Phase 3 (PR3: product-list) — grid, search, empty/error
// states are implemented there. It exists now only so the app-shell router
// (Phase 2) has a real element to mount at "/".
function ProductListPage() {
  return <div data-testid="product-list-page">Product list (coming in PR3)</div>
}

export default ProductListPage
